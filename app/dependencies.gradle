dependencies {
    // networking
    def okhttpVersion = '3.9.1'
    compile "com.squareup.okhttp3:okhttp:${okhttpVersion}"
    compile "com.squareup.okhttp3:logging-interceptor:${okhttpVersion}"

    compile 'org.java-websocket:Java-WebSocket:1.3.6'
    compile 'com.coolerfall:android-http-download-manager:1.6.1'

    // images
    def glideVersion = '4.4.0'
    compile "com.github.bumptech.glide:glide:${glideVersion}"
    compile "com.github.bumptech.glide:okhttp3-integration:${glideVersion}"
    annotationProcessor "com.github.bumptech.glide:compiler:${glideVersion}"

    // architecture
    compile 'com.birbit:android-priority-jobqueue:2.0.1'
    compile 'org.greenrobot:eventbus:3.1.1'

    // json
    compile 'com.google.code.gson:gson:2.8.2'

    // json:api
    def retrofitVersion = '2.3.0'
    compile "com.squareup.retrofit2:retrofit:${retrofitVersion}"
    compile "com.squareup.retrofit2:converter-moshi:${retrofitVersion}"
    compile 'moe.banana:moshi-jsonapi:3.3.0'

    // user interface
    compile 'com.github.barteksc:android-pdf-viewer:2.8.1'
    compile 'de.psdev.licensesdialog:licensesdialog:1.8.3'
    compile 'com.github.Commit451:bypasses:1.1.0'

    // video
    compile 'com.devbrackets.android:exomedia:4.0.3'

    // view binding
    def butterknifeVersion = '8.8.1'
    compile "com.jakewharton:butterknife:${butterknifeVersion}"
    annotationProcessor "com.jakewharton:butterknife-compiler:${butterknifeVersion}"

    // field binding
    def autobundleVersion = '4.1.0'
    compile "com.github.yatatsu:autobundle:${autobundleVersion}"
    annotationProcessor "com.github.yatatsu:autobundle-processor:${autobundleVersion}"

    // play services
    def playVersion = '11.6.2'
    compile "com.google.android.gms:play-services-cast-framework:${playVersion}"

    // support libraries
    def supportVersion = '27.0.2'
    compile "com.android.support:support-v4:${supportVersion}"
    compile "com.android.support:appcompat-v7:${supportVersion}"
    compile "com.android.support:cardview-v7:${supportVersion}"
    compile "com.android.support:recyclerview-v7:${supportVersion}"
    compile "com.android.support:mediarouter-v7:${supportVersion}"
    compile "com.android.support:design:${supportVersion}"
    compile "com.android.support:preference-v14:${supportVersion}"

    // local libraries
    implementation project(path: ':lanalytics', configuration: 'default')

    compile fileTree(include: ['*.jar'], dir: 'libs')
}
